{
  "name": "growbook-prototype",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --env-file-if-exists=server/.env server/index.mjs",
    "test": "node --test tests/*.test.mjs",
    "build": "node scripts/build.mjs",
    "check": "node --check tutor/app.js && node --check server/index.mjs"
  },
  "engines": { "node": ">=22" }
}
