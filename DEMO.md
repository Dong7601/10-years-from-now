# 音声AI学習入口 · 当日デモ手順

## 起動

Node.js 22以上を使用。追加パッケージのインストールは不要です。

1. `server/.env.example` を `server/.env` にコピー。
2. `OPENAI_API_KEY=` の右側にOpenAI Platformで発行したキーを設定。チャットには貼らない。
3. APIプロジェクトに利用可能なクレジットがあることを確認。ChatGPTの契約とは別です。
4. このリポジトリで `npm start`。
5. PC Chromeで `http://localhost:3000/tutor/` を開く。

`.env` を変更したらサーバーを再起動してください。キーはGit管理対象外で、静的配布対象にも入りません。

## 5分の実演

1. 同意にチェック。画面共有を使う場合はChromeの確認画面で「このタブ」またはGROWBOOKを選ぶ。呼び出し元タブを候補に含めて優先表示する設定済み。確認画面自体はChromeの安全仕様により省略できない。
2. 「記録を始めて、問題を解く」を押し、マイクを許可する。これは録音用で、この段階では音声AIへ接続しない。
3. 生徒は黙って3問を解く。AIは挨拶・質問・ヒントを出さない。
4. 迷ったら少し考えたり、選び直したりする。実際に起きた操作と時刻を記録する。デモでも起きていない停止や誤答を捏造しない。
5. 画面共有中は録画と時刻付き静止画を端末に保存する。画像は解答終了後に送る。共有していない場合は操作ログだけの縮退モード。
6. 3問を終えて「AIと振り返る」。AIは録画の静止画・操作・採点を確認し、「分からないところはあった？」や、記録に基づく確認質問を音声で始める。
7. 生徒は「whatとthatで迷った」「そこは考えていたんじゃなくて離席していた」など、普通に会話する。AIは観測と本人の説明を区別して補完する。「サッカーの例えで説明して」と希望したら、提案カードを承認して教科書へつなぐ。
8. 「わたしの教科書」に生成された説明案を確認する。元の英語モックは上書きしない。
9. 「学習を終了」。録音（自分・AI別）、画面録画、JSONをそれぞれ保存する。

## 必要なもの

| 項目 | 用途 |
| --- | --- |
| OpenAI APIキー＋APIクレジット | Realtime音声会話、Responsesによる教科書・類題生成 |
| 対象モデルの利用権限 | 既定は `gpt-realtime-2.1` / `gpt-5.6-sol`。利用可能なモデルは環境変数で指定 |
| PCのマイク・ブラウザの許可 | 生徒の声の送信と録音 |
| 画面共有の許可（任意） | 画面録画と最大30枚の静止画送信 |
| GitHubの書き込み権限 | リポジトリへのアップロード |

追加の音声認識サービス・データベース・クラウドストレージの契約は不要です。

## よくある状態

- **APIの残高不足**：OpenAI PlatformでAPIクレジットを追加するか、利用可能なキーへ変更。エラーを成功表示に置き換えることはしません。
- **マイクを拒否**：文字入力で続行し、サイトのマイク権限を戻して「音声につなぐ」。
- **画面共有を拒否／iPad**：画面録画と画像理解は使えません。解答・会話の記録と音声は別です。iPad実機での音声検証は未実施。
- **GitHub Pages**：公開できるのは静的プレビューだけです。ブラウザにAPIキーを入力する仕組みはありません。実音声デモはローカルサーバーで行います。
- **外部公開したい**：このサーバーは127.0.0.1に限定し、localhost以外のAPI呼び出しを拒否します。一般公開には認証、HTTPS、ユーザー別利用上限が必要です。単にHOSTを変えて公開しないでください。
- **教材更新**：既存英語モックへのデータ取込ではなく、同じリポジトリに追加した英語教科書画面で更新案を表示します。JSONの問題ID・段落IDを後続チームへ渡せます。

## 記録とデータ

- 正誤はサーバーで採点。未回答は `null`。第1/2/3問は指定英語サイトのQ1/Q2/Q3（内部IDは `eng-q1/eng-q2/eng-q3`）。
- 変更履歴、表示・非表示時間、会話字幕、提案の承認／却下、生成結果を端末のIndexedDBへ保存。
- メディアは2秒ごとのチャンクとして端末保存。終了時に同じ録音ごとに結合してダウンロード。自分とAIの音声は別ファイルで、時刻情報はJSONに保持。
- 音声・画像・会話をAPIへ送信することと、録音ファイルを端末に保存することは別です。Responsesでは `store:false` を使用していますが、API側のデータ取扱い全体をゼロ保持と保証するものではありません。
- 字幕は自動認識・生成の記録です。割り込み時に聞こえなかった生成文が含まれる場合があり、理解の根拠として断定しないでください。
- 録画ファイル全体をAPIにアップロードする構成ではなく、録画ストリームから採取した静止画（最大960px・3秒以上の間隔・最大30枚）を使います。問題切替・解答変更と10秒間隔で採取し、終了後に各問題の先頭・中間・末尾から最大9枚を解析します。静止画間の動きまで連続動画として理解したとは主張しません。
- プロトタイプではブラウザ更新からの学習再開は未対応です。終了前にページを閉じず、終了後に記録をダウンロードしてください。残った保存データはブラウザのサイトデータ設定からも削除できます。
- 実在の生徒による運用前に、保護者同意、保存期間、アクセス権、削除手順を別途整備してください。当日は架空の学習データで実演します。

## 検証コマンド

```sh
npm test
npm run check
npm run build
```

テストは採点、入力検証、秘密ファイル非公開、Origin制限、模擬APIによる生成・音声接続設定を確認します。模擬APIの成功は実際の音声会話の成功を意味しません。

## 参照

- [OpenAI Realtime WebRTC](https://developers.openai.com/api/docs/guides/voice-webrtc?api=realtime)
- [Realtime conversations](https://developers.openai.com/api/docs/guides/realtime-conversations)
- [Realtime model](https://developers.openai.com/api/docs/models/gpt-realtime-2.1)
