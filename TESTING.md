# GROWBOOK 検証記録

確認日: 2026-09-13。題材は https://manabiai.netlify.app/ の英語Q1〜Q3。

## 確認済み

- `npm test`: 6件成功。採点・未回答・未知ID・秘密ファイル非公開・Origin制限・画像の入力検証・模擬APIの契約を確認。
- `npm run check`: JavaScript構文検証成功。
- `npm run build`: distへの静的ファイル出力成功。server/.envやサーバーコードは出力されない。
- ブラウザ: GROWBOOK表記、英語の問題文と4選択肢、3問の回答・採点・記録画面を確認。
- 解答中: 音声セッションのHTTP呼び出し0件、質問入力が無効、AI待機を確認。
- 実OpenAI API: クレジット追加後、観察メモ・テキスト応答・RealtimeのSDP交換・音声出力・関数呼び出しが成功。
- 英語版で「whatを何という意味で使えると思った」と文字で説明し、AIが提案したカードを承認。実Responses APIによるthat/whatの説明案が教科書に追加された。
- 終了操作後: 接続・記録の停止表示、AI音声ファイルの保存ボタンを確認。テストのAI音声は約2.9MB。
- 最新のチーム更新を取り込み、既存 `netlify/functions/chat.mjs` は変更なし。新APIは `/api/tutor/` に分離。

## まだ確認していないこと

- 実マイクでの発話認識、対面と同程度の往復感、音声割り込みの実機確認。
- 実際の画面共有ピッカー、画面録画ファイルの再生、保存した画面フレームの視覚解析を通した一連の実演。
- iPad実機、20分間の耐久、通信断後の完全復元、映像と両者音声の完全同期。
- GitHub pushと公開サイトへの反映。書き込み権限が必要。

音声接続テストには無音の合成マイクを使い、周囲の会話は録音していません。入力の説明は文字で送っています。したがって「実マイクを使った双方向音声デモまで検証済み」とは扱いません。解答中の録音と終了後の音声接続・保存処理を分けて検証しています。

## 当日最初に行う実機確認

発表直前の優先ルートはREADMEの**テキスト入力**です。画面共有のチェックを外し、「音声記録なしで試す（振り返りは文字）」を選んでください。以下の実機確認が未完了でも、確認済みの文字入力から教科書生成までを実演できます。

PC Chromeで `http://localhost:3000/tutor/` を開き、共有画面の「Chrome タブ」から「このタブ」またはGROWBOOKを選び、マイクを許可して開始。共有をキャンセルした場合はマイクを開始せず、開始画面へ戻る。黙って解答し、終了後の問いかけに声で答え、教科書への提案を承認する。終了後に画面録画と双方の録音を保存・再生する。この実機確認が通ってから、録画を含む完全デモの準備完了と判断する。

## チーム更新の統合と静的モック

Geminiの教科書生成APIと5単元の既定教科書の更新を取り込みました。別タスク作成の `mock/textbook.html`・`mock/workbook.html` は内容を変更せず同梱し、ローカル配信・静的ビルドの対象へ追加しています。3つのHTML（既存トップと両モック）のインラインJavaScript構文、自動テスト6件、ビルドを再確認しました。
